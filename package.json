{
  "name": "generate-intervals",
  "version": "1.0.0",
  "description": "A simple module to generate an array containing period objects for a specified interval",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "period",
    "interval",
    "array"
  ],
  "author": "Jorg van der Ham",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.9.0"
  }
}
